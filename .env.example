# Public API URL used by the browser
NUXT_PUBLIC_API_BASE_URL=https://admin2.uzbekbusinessconnect.com/api
NUXT_PUBLIC_API_URL=https://admin.uzbekbusinessconnect.com/api

# Server-side API URL (SSR). Use an internal/reachable host if the public API
# is not reachable from your Docker host (e.g. ETIMEDOUT on admin2).
NUXT_API_BASE_URL=https://admin2.uzbekbusinessconnect.com/api
